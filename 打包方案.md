# 打包方案

> 目标：把 agent-platform 打包成一个**发给不会装环境的人也能双击运行的应用**（目标机无 Docker、无 JDK、无 Maven、无 MySQL）。
> 约束优先级：**改动最小 > 出错最少 > 体验最好**。
> 结论依据：本文档对照 2026-09-08 的代码现状核实（不是照抄 `docs/feasibility.md` 的归档结论）。

---

## 0. 结论速览

| 项 | 结论 |
|---|---|
| **推荐方案** | **方案 B：Windows 绿色免安装包（Portable Bundle）** —— 内嵌 JRE + 内嵌便携 MySQL + 启动器脚本 |
| **源码改动** | **零**（Java / 前端 / `application.yml` / Flyway 脚本全部不动） |
| **新增文件** | 1 个打包脚本 + 3 个启动器脚本 + 1 个 `my.ini` 模板（共 ~5 个文件） |
| **数据库** | **继续用 MySQL 8.4**（以官方 no-install ZIP 内嵌，首次运行自动初始化）。**不换 H2/SQLite** |
| **交付物** | 一个文件夹（约 700MB–1GB，压缩后 350–450MB），`启动.bat` 双击即用 |
| **不推荐** | jpackage `.exe/.msi`（要先做 DB 内置化）、内嵌 H2（改动最大）、Electron/Tauri 壳（不解决真正矛盾）、GraalVM native-image（与插件 SPI 冲突） |

**一句话理由**：本项目的唯一硬依赖是 MySQL，其余中间件（Redis/Milvus/Kafka/MinIO/LiteLLM）默认全关且有内存兜底；而 Flyway 的 10 个迁移脚本深度绑定 MySQL 方言。所以"改动最小"的打包 = **不动一行业务代码，只把 JRE 和 MySQL 一起装进文件夹，用脚本串起来**。

---

## 1. 打包相关的硬事实（核实自当前代码）

| 事实 | 证据 | 对打包的影响 |
|---|---|---|
| 单一 fat jar 即完整应用 | `agent-platform-core/target/agent-platform-core-1.0.0-SNAPSHOT.jar` = **145 MB**，前端产物已内嵌在 `src/main/resources/static/` | 只需分发 1 个 jar，无需 Node/nginx |
| 前端用 HashRouter | `agent-platform-ui/src/main.tsx:12` | **不需要 SPA fallback**，也不需要 nginx |
| 必须 `--enable-preview` | `pom.xml:196` 编译带 preview；README 明示 `ScopedValue` | 运行 JRE 必须是 **JDK/JRE 21 且版本号一致**，启动参数必须带该 flag |
| MySQL 是唯一硬依赖 | `application.yml:24-27`，Flyway 连不上即启动失败 | 必须随包携带或要求预装 |
| MySQL 方言深度耦合 | 迁移脚本含 `ENGINE=InnoDB`×21、`utf8mb4`×42、`MEDIUMTEXT`×10、`ON UPDATE CURRENT_TIMESTAMP`×11、`ALTER ... AFTER`×3、`FULLTEXT ... WITH PARSER ngram`×1、`JSON`×30 | 换内嵌库 = 重写 10 个脚本 + 检索层方言分支，**改动量最大** |
| 混合检索有全表扫描兜底 | `HybridRetriever.java:140-166` | 说明 FULLTEXT 可退化，但不改变"换库仍需重写 DDL"的结论 |
| 其余中间件默认全关 | `application.yml:131,134,140,146`（security/events/vector-store/storage 默认 false/in-memory/local） | 打包时无需 Redis/Kafka/Milvus/MinIO |
| 数据目录是相对路径 | `./data/skills`、`./data/files`、`./data/plugins` | 启动器必须 `cd` 到应用目录，否则数据落到别处 |
| 网关模块独立且非必需 | `agent-platform-gateway` 是 Spring Cloud Gateway，不在核心链路上 | 单机包**不打包** gateway |
| `agent-platform-deploy/server/` 是空占位 | 该目录 7 个文件均为 **0 字节** | 本方案的新脚本就落在这里，不冲突 |

---

## 2. 候选方案对比

| # | 方案 | 交付物 | 源码改动 | 目标机前置 | 出错概率 | 体积 | 结论 |
|---|---|---|---|---|---|---|---|
| A | **Docker Compose 一体包** | 文件夹 + 脚本（core 镜像 + mysql 容器） | 0 | Docker Desktop | 低 | 镜像 ~1.2GB | ⚠️ 省力但**目标机无 Docker，已排除**（自用/服务器仍可选） |
| **B** | **绿色免安装包**（内嵌 JRE + 便携 MySQL） | 文件夹，`启动.bat` 双击 | **0** | 无（仅需 Win10/11 x64） | **中低** | 解压 ~700MB–1GB | ✅ **推荐** |
| C | jpackage `.exe/.msi` 安装程序 | 安装包 + 开始菜单 + 卸载 | 需 DB 内置化 | 无 | **高** | ~250MB | ❌ 前置是"先解决 DB 内置化"，改动大且 jpackage 与 Spring Boot 嵌套 jar 有兼容坑 |
| D | 内嵌 H2(MODE=MySQL) / SQLite | 文件夹 | **大**（10 个迁移 + 检索层 + Flyway 方言） | 无 | **高** | ~250MB | ❌ 与"改动最小"直接冲突，见 §4 |
| E | Electron / Tauri 桌面壳 | 桌面安装包 | 中（壳）+ 仍需解决 DB | 无 | 中 | 大/小 | ❌ 壳不解决真正的矛盾（DB），白加一层 |
| F | GraalVM native-image | 单个原生 exe | 大 | 无 | 高 | ~80MB | ❌ 与插件 SPI / ClassLoader 动态加载冲突（`PluginClassLoader`、`ExternalPluginLoader`） |

> 说明：B 与 A 的差异只在"MySQL 怎么来"。A 用容器，B 用官方 no-install ZIP 直接跑 `mysqld.exe`。

---

## 3. 推荐方案：Windows 绿色免安装包

### 3.1 交付物目录结构

```
agent-platform-1.0.0-win64/
├── 启动.bat                     # 双击入口：初始化 → 起 MySQL → 起 core → 开浏览器
├── 停止.bat                     # 停 core + 停 MySQL
├── 重置数据.bat                 # （可选）清空 data/，回到初始状态
├── 说明.txt                     # 给最终用户看的一页说明
├── app/
│   └── agent-platform-core-1.0.0-SNAPSHOT.jar    # 145MB，含前端
├── runtime/                     # 内嵌 JRE 21（Temurin，~130MB）
│   └── bin/java.exe
├── mysql/                       # MySQL 8.4 no-install ZIP 解包（裁剪后 ~350–400MB）
│   └── bin/{mysqld.exe, mysql.exe, mysqladmin.exe}
├── conf/
│   ├── my.ini                   # 首次运行生成（写入绝对路径 + 端口 3307）
│   └── app.env                  # 首次运行生成（密钥 + DB 口令，切勿分发后泄露）
└── data/                        # 首次运行自动创建，全部运行时数据都在这里
    ├── mysql/                   # MySQL datadir（持久化，勿删）
    ├── files/  skills/  plugins/
```

**关键设计**：
- **MySQL 端口用 3307**（不是 3306），避免与目标机上已有的 MySQL 冲突。
- **`data/` 是唯一需要备份的目录**，重装/升级只替换 `app/` 和 `runtime/`。
- **`conf/app.env` 首次运行生成随机密钥**，之后固定不变（`MODEL_KEY_ENC_KEY` 一旦变化，仪表盘里已加密的模型 API Key 全部解不开）。

### 3.2 新增/修改文件清单（这就是"改动最小"的全部）

| 动作 | 路径 | 说明 |
|---|---|---|
| 新增 | `agent-platform-deploy/portable/package-portable.bat` | **开发机**上执行，产出上面的交付文件夹 |
| 新增 | `agent-platform-deploy/portable/templates/启动.bat` | 启动器模板 |
| 新增 | `agent-platform-deploy/portable/templates/停止.bat` | 停止器模板 |
| 新增 | `agent-platform-deploy/portable/templates/my.ini.tpl` | MySQL 配置模板 |
| 新增 | `agent-platform-deploy/portable/README.md` | 打包与交付说明 |
| 修改 | `.gitignore` | 加一行 `dist-portable/` |

**Java 源码、前端源码、`application.yml`、Flyway 迁移脚本：零改动。**

### 3.3 启动器核心逻辑（`启动.bat`）

```bat
@echo off
setlocal enabledelayedexpansion
cd /d "%~dp0"                        REM 关键：数据目录是相对路径
chcp 65001 >nul 2>&1                 REM 控制台 UTF-8，避免中文日志乱码
title Agent Platform

REM ── 0) 路径自检：中文/空格路径在 MySQL 上有历史坑 ──
echo %CD% | findstr /r "[^ -~]" >nul && (
  echo [WARN] 当前路径含中文或空格，建议解压到纯英文路径（如 D:\agent-platform）
)

REM ── 1) 首次运行：生成 conf\app.env（随机密钥，之后固定） ──
if not exist "conf\app.env" (
  echo [1/6] 首次运行，生成配置与密钥...
  "runtime\bin\java.exe" -version >nul 2>&1 || (echo [ERROR] 内嵌 JRE 缺失 & pause & exit /b 1)
  for /f %%i in ('powershell -NoProfile -Command "-join ((48..57)+(97..122) | Get-Random -Count 32 | %%{ [char]%%_ })"') do set "GEN=%%i"
  > "conf\app.env" echo MYSQL_PASSWORD=!GEN!
  >> "conf\app.env" echo MODEL_KEY_ENC_KEY=!GEN!!GEN!
  >> "conf\app.env" echo JWT_SECRET=!GEN!!GEN!!GEN!
)

REM ── 2) 首次运行：生成 conf\my.ini（写入绝对路径） ──
if not exist "conf\my.ini" (
  set "BASE=%CD:\=/%"
  > "conf\my.ini" echo [mysqld]
  >> "conf\my.ini" echo basedir=!BASE!/mysql
  >> "conf\my.ini" echo datadir=!BASE!/data/mysql
  >> "conf\my.ini" echo port=3307
  >> "conf\my.ini" echo bind-address=127.0.0.1
  >> "conf\my.ini" echo character-set-server=utf8mb4
  >> "conf\my.ini" echo collation-server=utf8mb4_0900_ai_ci
  >> "conf\my.ini" echo default-time-zone=+08:00
  >> "conf\my.ini" echo skip-name-resolve
  >> "conf\my.ini" echo max_connections=50
)

REM ── 3) 首次运行：初始化 MySQL 数据目录（约 30–60 秒） ──
if not exist "data\mysql\mysql" (
  echo [2/6] 初始化内置数据库，首次运行请耐心等待...
  "mysql\bin\mysqld.exe" --defaults-file="conf\my.ini" --initialize-insecure --console
  if errorlevel 1 (echo [ERROR] 数据库初始化失败 & pause & exit /b 1)
)

REM ── 4) 启动 MySQL（已在跑则跳过） ──
netstat -ano | findstr ":3307" | findstr LISTENING >nul 2>&1
if errorlevel 1 (
  echo [3/6] 启动内置数据库（端口 3307）...
  start "agent-mysql" /min "mysql\bin\mysqld.exe" --defaults-file="conf\my.ini"
  REM 轮询等待就绪，最多 60 秒
  for /l %%i in (1,1,60) do (
    "mysql\bin\mysqladmin.exe" -h 127.0.0.1 -P 3307 -u root ping >nul 2>&1 && goto :db_ready
    timeout /t 1 /nobreak >nul
  )
  echo [ERROR] 数据库启动超时 & pause & exit /b 1
)
:db_ready

REM ── 5) 建库建号（幂等；root 首次为空口令，随后设随机口令） ──
echo [4/6] 准备数据库与账号...
for /f "tokens=1,* delims==" %%a in ('type "conf\app.env"') do set "%%a=%%b"
"mysql\bin\mysql.exe" -h 127.0.0.1 -P 3307 -u root --skip-password -e ^
"CREATE DATABASE IF NOT EXISTS agent_platform DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci; ^
 CREATE USER IF NOT EXISTS 'agent'@'%%' IDENTIFIED BY '!MYSQL_PASSWORD!'; ^
 GRANT ALL PRIVILEGES ON agent_platform.* TO 'agent'@'%%'; ^
 FLUSH PRIVILEGES;"
"mysql\bin\mysqladmin.exe" -h 127.0.0.1 -P 3307 -u root password "!MYSQL_PASSWORD!" >nul 2>&1

REM ── 6) 启动 core（Flyway 自动建表） ──
echo [5/6] 启动应用（端口 8081）...
set "MYSQL_URL=jdbc:mysql://127.0.0.1:3307/agent_platform?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true"
set "MYSQL_USER=agent"
set "SECURITY_ENABLED=false"
echo [6/6] 浏览器将自动打开 http://localhost:8081/ ，关闭本窗口即停止应用
start "" http://localhost:8081/
"runtime\bin\java.exe" --enable-preview -XX:+UseZGC -Xms256m -Xmx1g ^
  --add-opens java.base/java.lang=ALL-UNNAMED ^
  -jar "app\agent-platform-core-1.0.0-SNAPSHOT.jar"
```

`停止.bat` 两行即可：

```bat
@echo off
cd /d "%~dp0"
mysql\bin\mysqladmin.exe -h 127.0.0.1 -P 3307 -u root -p shutdown
for /f "tokens=5" %%P in ('netstat -ano ^| findstr ":8081" ^| findstr "LISTENING"') do taskkill /F /PID %%P >nul 2>&1
```

### 3.4 打包脚本做什么（`package-portable.bat`，在开发机跑）

```bat
1. mvn -q -pl agent-platform-core -am package -DskipTests        REM 产出 fat jar
2. 拷 jar            -> dist-portable/app/
3. 解压 Temurin 21 JRE zip -> dist-portable/runtime/
4. 解压 MySQL 8.4 winx64 zip -> dist-portable/mysql/（删掉 mysql-test/、docs/、*.pdb 瘦身）
5. 拷 templates/*     -> dist-portable/
6. 附上 LICENSE（MySQL GPLv2）+ 说明.txt
7. 7z 打包为 agent-platform-1.0.0-win64.zip
```

**前置下载（一次，放开发机 `~/tools/`）**：
- Temurin 21 JRE（Windows x64 zip）—— 注意要 **21**，不能是 17/24，否则 `--enable-preview` 的 class 版本不匹配
- `mysql-8.4.x-winx64.zip`（官方 no-install 包）

### 3.5 首次运行流程（最终用户视角）

```
解压 zip → 双击「启动.bat」→ 等 1–2 分钟（首次初始化 + Flyway 建 10 张表）
→ 浏览器自动打开 http://localhost:8081/ → 在「模型设置」页填 API Key 即可用真实模型
（不填也能用：对话走本地 Mock，RAG 走内存伪向量）
```

---

## 4. 为什么数据库继续用 MySQL（不换 H2）

| 维度 | 继续 MySQL（推荐） | 换 H2(MODE=MySQL) |
|---|---|---|
| Flyway 迁移脚本 | **0 改动** | 需重写 `V1–V10`：`MEDIUMTEXT`→`TEXT`、`DATETIME(3)`→`TIMESTAMP(3)`、`ALTER ... AFTER` 不支持、`ENGINE=InnoDB` 语法、`ON UPDATE CURRENT_TIMESTAMP` 语义差异、**`FULLTEXT ... WITH PARSER ngram` 无对应** |
| 检索层 | 0 改动 | `HybridRetriever` 的 ngram 候选集查询需加方言分支（虽有全表扫描兜底，但等于降级） |
| JSON 列 | 原生 JSON | `@JdbcTypeCode(SqlTypes.JSON)` 在 H2 上映射为 CLOB，30 处 JSON 列需回归验证 |
| 回归测试面 | 现有 104 个测试直接有效 | 全部集成测试（Testcontainers MySQL）需换库重跑 |
| 包体积 | +350MB（MySQL） | −350MB |
| 出错概率 | 低（与开发环境完全同构） | **高** |
| 结论 | ✅ | ❌ |

**判断**：包体积是"一次性下载成本"，而方言适配是"持续出错成本"。在"改动最小、出错最少"的优先级下，**多 350MB 换取零代码改动是划算的**。
唯一例外：如果最终交付要对外商用分发，MySQL 社区版是 GPLv2，需随包附许可证；内部/私有交付无碍。若对外分发有法务顾虑，再评估 MariaDB（**注意**：MariaDB 不支持 `WITH PARSER ngram`，仍需改 `V2` 迁移）。

---

## 5. 关键坑与规避（打包前必读）

| # | 坑 | 后果 | 规避 |
|---|---|---|---|
| 1 | `MODEL_KEY_ENC_KEY` 每次启动都变 | 仪表盘里已存的模型 API Key 全部解不开 | 首次运行生成后**持久化到 `conf/app.env`**，之后只读不生成 |
| 2 | 启动器没 `cd` 到应用目录 | `data/`、`./data/skills` 落到别处，数据"丢失" | 脚本首行 `cd /d "%~dp0"` |
| 3 | 目标机已有 MySQL 占 3306 | 端口冲突，起不来 | 内嵌 MySQL 固定用 **3307**，且 `bind-address=127.0.0.1` |
| 4 | JRE 版本不是 21 | `UnsupportedClassVersionError` / preview 特性报错 | 打包脚本强校验 `runtime\bin\java.exe -version` 必须是 21.x |
| 5 | 漏了 `--enable-preview` | 启动即 `UnsupportedOperationException`（ScopedValue） | 启动器硬编码该参数 |
| 6 | 解压到中文/带空格路径 | MySQL 初始化失败或路径解析异常 | 启动器检测并 WARN；`说明.txt` 写明建议 `D:\agent-platform` |
| 7 | 目标机缺 VC++ 2019 运行库 | `mysqld.exe` 直接闪退 | `说明.txt` 给出下载指引；启动器检测 `mysqld --version` 失败时提示 |
| 8 | 杀软/Defender 拦 `mysqld.exe` | 数据库起不来 | 说明里提示加白名单 |
| 9 | 8081 被占用 | 应用起不来 | 启动器先探测并提示，或 `taskkill` 残留 |
| 10 | 首次启动慢（Flyway 10 个迁移 + Skills 铺样例） | 用户以为卡死 | 脚本打印进度；说明里写"首次 1–2 分钟" |
| 11 | 分发时误带 `conf/app.env` | 密钥泄露 | `package-portable.bat` 明确**不拷贝** `conf/`；`.gitignore` 排除 |
| 12 | 升级时覆盖 `data/` | 数据丢失 | 说明里写明"升级只替换 `app/`、`runtime/`、`mysql/`" |

---

## 6. 实施步骤

### 阶段 0：手工验证（0.5 天，先证明可行）
1. 开发机下载 Temurin 21 JRE zip、`mysql-8.4.x-winx64.zip`，放到 `~/tools/`；
2. 手工按 §3.1 拼一个文件夹，手工跑一遍 `mysqld --initialize-insecure` → 起 core；
3. 验收：`http://localhost:8081/` 能打开仪表盘，新建智能体 + 对话运行返回 Mock 回复。

### 阶段 1：脚本化（1 天）
4. 写 `package-portable.bat` + 三个模板脚本，落到 `agent-platform-deploy/portable/`；
5. 在**一台干净的 Windows**（或新建本地用户 + 清空环境变量）上从 zip 完整走一遍冷启动；
6. 补 `说明.txt`、许可证。

### 阶段 2：交付（0.5 天）
7. 打 zip，写一页《使用说明》给接收方；
8. 记录已知限制：单机单用户、`SECURITY_ENABLED=false`（本地无鉴权）、建议仅内网/本机使用。

### 验收清单
```bash
# 1. 干净机器上解压 → 双击启动.bat → 等待完成
# 2. 健康检查（redis 可为 DOWN，属正常）
curl http://localhost:8081/actuator/health
# 3. 仪表盘可用
#    浏览器打开 http://localhost:8081/ → 新建智能体 → 对话运行 → 得到 Mock 回复
# 4. 重启幂等：关闭 → 再次双击启动.bat → 数据（智能体/会话）仍在
# 5. 停止：双击停止.bat → 8081 与 3307 均无监听
```

---

## 7. 后续可选升级（都不影响本方案先行落地）

| 升级项 | 收益 | 代价 |
|---|---|---|
| Inno Setup 做安装程序 | 双击安装、开始菜单、卸载 | 比 jpackage 简单可靠，可直接把 MySQL 塞进安装目录；~1 天 |
| jlink 瘦身 runtime | 省 ~50MB | 需做全功能冒烟测试（漏模块 = 运行时 `NoClassDefFoundError`）；建议先发布全量 JRE 版 |
| 内置数据库（H2） | 省 ~350MB、免起 MySQL 进程 | 见 §4，改动大，**当前不做** |
| Docker 一体包（方案 A） | 服务器/自用场景更省心 | 目标机需 Docker；本方案可作为并行产物一起出 |

---

## 8. 一句话总结

**不动一行业务代码**：把已构建好的 145MB fat jar、一个内嵌 JRE 21、一份官方 MySQL 8.4 no-install 包放进同一个文件夹，用 `启动.bat` 把「初始化 DB → 起 MySQL(3307) → 起 core(8081) → 开浏览器」串起来——这就是改动最小、出错最少、且能交给"什么都不会装"的人的方案。真正的风险不在打包本身，而在**首次运行的环境初始化**（§5 的 12 条），所以阶段 0 一定要先在一台干净机器上冷启动验证一遍。
