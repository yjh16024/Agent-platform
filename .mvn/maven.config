# Shared Maven flags for this repo (keep ASCII-only: Maven reads this file as UTF-8).
# Retry 3 times on flaky network to avoid sporadic "Connection reset".
-Dmaven.wagon.http.retryHandler.count=3
# Keep connections alive 25s to reduce stale-connection timeouts.
-Dmaven.wagon.httpconnectionManager.ttlSeconds=25
