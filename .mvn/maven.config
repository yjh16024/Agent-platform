# Maven 构建共享参数（作用于本仓库所有构建，勿写环境相关信息）
# 网络抖动时自动重试 3 次，规避偶发 Connection reset（曾致 spring-boot-maven-plugin 拉取失败）
-Dmaven.wagon.http.retryHandler.count=3
# 连接保活 25s，减少复用陈旧连接导致的超时
-Dmaven.wagon.httpconnectionManager.ttlSeconds=25
